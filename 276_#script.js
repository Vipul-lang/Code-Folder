console.log("2"+1+1);
console.log(2+1+"1");

//Expression evalution order Left to Right

// String + Anything = String (Concatenation)
// Number + Number = Number (Addition)
// Operations are evaluated from left to right.
// If a string appears first in a + operation, everything becomes a string.